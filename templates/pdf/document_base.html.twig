<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Document{% endblock %}</title>
    <style>
        /* Reset et configuration de base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            font-size: 11px;
            line-height: 1.2;
            color: #000;
            background: #fff;
        }

        .page {
            width: 210mm;
            min-height: 297mm;
            padding: 15mm;
            margin: 0 auto;
            background: white;
            position: relative;
        }

        /* En-tête avec logos */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 30px;
            height: 80px;
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo-france-patrimoine {
            width: 120px;
            height: auto;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-sma, .logo-qualibat {
            width: 80px;
            height: auto;
        }

        /* Titre du document */
        .document-title {
            text-align: center;
            margin: 20px 0 30px 0;
        }

        .document-title h1 {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .document-number {
            font-size: 12px;
            color: #333;
        }

        /* Informations client */
        .client-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 25px;
        }

        .client-left, .client-right {
            width: 48%;
        }

        .client-left {
            font-size: 11px;
            line-height: 1.4;
        }

        .client-right {
            font-size: 11px;
            line-height: 1.4;
            text-align: left;
        }

        .client-right strong {
            font-weight: bold;
        }

        /* Tableau des prestations */
        .prestations-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-size: 10px;
        }

        .prestations-table th {
            background-color: #f5f5f5;
            border: 1px solid #333;
            padding: 8px 5px;
            text-align: center;
            font-weight: bold;
            font-size: 10px;
        }

        .prestations-table td {
            border: 1px solid #333;
            padding: 8px 5px;
            vertical-align: top;
        }

        .prestations-table .prestation-name {
            font-weight: bold;
            width: 20%;
        }

        .prestations-table .description {
            width: 35%;
            font-size: 9px;
            line-height: 1.3;
        }

        .prestations-table .unite, .prestations-table .prix-unitaire,
        .prestations-table .quantite, .prestations-table .total {
            text-align: center;
            width: 11.25%;
        }

        /* Section remarques */
        .remarques-section {
            margin: 25px 0;
        }

        .remarques-title {
            background-color: #f5f5f5;
            border: 1px solid #333;
            padding: 8px;
            font-weight: bold;
            font-size: 11px;
        }

        .remarques-content {
            border: 1px solid #333;
            border-top: none;
            padding: 12px;
            font-size: 10px;
            line-height: 1.4;
        }

        /* Garanties */
        .garanties-section {
            display: flex;
            gap: 20px;
            margin: 20px 0;
            font-size: 10px;
        }

        .garanties-left, .garanties-right {
            flex: 1;
            border: 1px solid #333;
            padding: 10px;
            background-color: #f9f9f9;
        }

        .garanties-left {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .garantie-item {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .checkbox {
            width: 12px;
            height: 12px;
            border: 1px solid #333;
            display: inline-block;
            position: relative;
        }

        .checkbox.checked:after {
            content: '✓';
            position: absolute;
            left: 2px;
            top: -2px;
            font-size: 10px;
            font-weight: bold;
        }

        /* Options de paiement */
        .paiement-section {
            margin: 15px 0;
            font-size: 10px;
        }

        .paiement-options {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        /* Section financière */
        .financial-section {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin: 30px 0;
            gap: 20px;
        }

        .conditions-generales {
            flex: 1;
            font-size: 9px;
            line-height: 1.4;
        }

        .signature-section {
            flex: 1;
        }

        .totaux-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            font-size: 11px;
        }

        .totaux-table td {
            border: 1px solid #333;
            padding: 8px;
        }

        .totaux-table .label {
            background-color: #f5f5f5;
            font-weight: bold;
            width: 60%;
        }

        .totaux-table .montant {
            text-align: right;
            width: 25%;
        }

        .totaux-table .mode {
            text-align: center;
            width: 15%;
        }

        .signature-box {
            border: 1px solid #333;
            height: 120px;
            padding: 10px;
            margin-top: 10px;
        }

        .signature-title {
            font-weight: bold;
            text-align: center;
            margin-bottom: 10px;
            font-size: 12px;
        }

        .signature-subtitle {
            font-size: 10px;
            margin-bottom: 5px;
        }

        /* Footer */
        .footer {
            position: absolute;
            bottom: 15mm;
            left: 15mm;
            right: 15mm;
            font-size: 8px;
            text-align: center;
            border-top: 1px solid #ccc;
            padding-top: 5px;
            line-height: 1.3;
        }

        .page-number {
            position: absolute;
            bottom: 5mm;
            right: 15mm;
            font-size: 9px;
        }

        /* Paraphe */
        .paraphe {
            position: absolute;
            bottom: 50px;
            right: 20px;
            width: 60px;
            height: 40px;
            border: 1px solid #ccc;
            font-size: 8px;
            text-align: center;
            padding: 5px;
        }

        /* Responsive adjustments for printing */
        @media print {
            body { margin: 0; }
            .page {
                margin: 0;
                box-shadow: none;
                page-break-after: always;
            }
            .page:last-child {
                page-break-after: auto;
            }
        }

        /* Classes utilitaires */
        .text-center { text-align: center; }
        .text-right { text-align: right; }
        .font-bold { font-weight: bold; }
        .mb-10 { margin-bottom: 10px; }
        .mb-15 { margin-bottom: 15px; }
        .mb-20 { margin-bottom: 20px; }
    </style>
</head>
<body>
    {% block content %}{% endblock %}
</body>
</html>